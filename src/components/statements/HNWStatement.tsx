import PrescribedStatement from "./PrescribedStatement";

/** High Net Worth Individual Investor Statement — SI 2024/301 Sch 3. Render only. */
const HNWStatement = () => (
  <PrescribedStatement version="FPO_SCH5_PT1_SI2024-301" />
);

export default HNWStatement;
